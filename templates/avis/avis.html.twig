{% extends 'backbase.html.twig' %}
{% block body %}
            <!-- Table Start -->
            <div class="container-fluid pt-4 px-4">
                
                        <div class="bg-secondary rounded h-100 p-4">
                            <h6 class="mb-4">Avis</h6>
                            <table class="table" style="width:900px;">
                            <thead>
                            <tr>
                            <th>Id Avis</th>
                            <th>Rating</th>
                            <th>Conducteur</th>
                            <th>Passager</th>
                            <th>Delete</th>

                            
                        
        </tr>
    </thead>
    <tbody>
        {% for a in avis %}
            <tr>
            <td>{{ a.getId() }}</td>
            <td>{{ a.getRating() }}</td>
            <td>{{ a.getIdConducteur()}}</td>
            <td>{{ a.getIdClient()}}</td>
                  <td>
            <form action="{{ path('app_avis_delete', {'id': a.getId()}) }}" method="POST" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_method" value="DELETE" />
                <button type="submit" class="btn btn-square btn-primary m-2"><i class="fa fa-trash"></i></button>
            </form>
        </td>
        
            </tr>
        {% endfor %}
    </tbody>
</table>

                     
                            </div>
                        </div>
                   
            <!-- Table End -->

{% endblock %}